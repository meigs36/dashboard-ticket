import './globals.css'
import { ThemeProvider } from './context/ThemeContext'
import ThemeToggle from '@/components/ThemeToggle'

export const metadata = {
  title: 'Dashboard Assistenza Ticket',
  description: 'Sistema gestione ticket e clienti',
}

export default function RootLayout({ children }) {
  return (
    <html lang="it" suppressHydrationWarning>
      <body className="bg-gradient-to-br from-gray-50 via-blue-50 to-purple-50 dark:from-gray-900 dark:via-blue-950 dark:to-purple-950 min-h-screen transition-colors duration-300">
        <ThemeProvider>
          <ThemeToggle />
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}     
