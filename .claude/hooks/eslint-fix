#!/bin/bash
INPUT=$(cat)
FILE_PATH=$(echo "$INPUT" | jq -r '.tool_input.file_path // empty')

if [ -z "$FILE_PATH" ]; then
  exit 0
fi

# Esegui ESLint solo su file JS/JSX/TS/TSX
if [[ "$FILE_PATH" =~ \.(js|jsx|ts|tsx)$ ]]; then
  npx eslint --fix "$FILE_PATH" 2>/dev/null || true
fi

exit 0
